name: Publish Guardrail

on:
  workflow_dispatch:

jobs:
  publish-guardrail:
    runs-on: ubuntu-latest

    steps:
      - name: Explain publishing status
        run: |
          echo "Automated publishing is intentionally disabled."
          echo "Use docs/PUBLISHING.md to prepare a manual release process first."
