# Public URLs are optional for local development. Set them only after the
# corresponding public deployment exists; do not put credentials in this file.
NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_STORYBOOK_URL=
