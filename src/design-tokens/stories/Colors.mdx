import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';
import { colors, primitiveColors, semanticColors } from '../colors';

<Meta title="Foundations/Colors" />

# Colors

FeitozaUI colors follow the Quiet Future / Neon Infrastructure direction: dark infrastructure surfaces, quiet text roles, and controlled neon accents.

`colors` remains available as the default compatibility export. New work should prefer `primitiveColors` for raw values and `semanticColors` for intent.

<div className="fzui-doc-hero">
  <p className="fzui-doc-eyebrow">Foundations / Color System</p>
  <p className="fzui-doc-lede">
    Dark mode is the primary expression of FeitozaUI. Cyan is the action and focus signal. Coral is an expressive accent, not the default action color.
  </p>
  <div className="fzui-doc-meta">
    <span className="fzui-doc-pill">Dark first</span>
    <span className="fzui-doc-pill">Semantic roles</span>
    <span className="fzui-doc-pill">Accessible contrast</span>
  </div>
</div>

## Primitive Colors

<ColorPalette>
  <ColorItem title="Blue Gray" subtitle="Infrastructure surfaces and text hierarchy" colors={Object.values(primitiveColors.blueGray)} />
  <ColorItem title="Cyan" subtitle="Action, focus, selection, and technical signal" colors={Object.values(primitiveColors.cyan)} />
  <ColorItem title="Coral" subtitle="Expressive brand accent" colors={Object.values(primitiveColors.coral)} />
  <ColorItem title="Amber" subtitle="Warning and caution" colors={Object.values(primitiveColors.amber)} />
  <ColorItem title="Green" subtitle="Success and healthy state" colors={Object.values(primitiveColors.green)} />
  <ColorItem title="Red" subtitle="Error, danger, and destructive state" colors={Object.values(primitiveColors.red)} />
  <ColorItem title="Indigo" subtitle="Informational signal" colors={Object.values(primitiveColors.indigo)} />
  <ColorItem title="Gray" subtitle="Light surfaces and secondary text" colors={Object.values(primitiveColors.gray)} />
</ColorPalette>

## Semantic Dark Mode

<ColorPalette>
  <ColorItem title="Background" subtitle={semanticColors.dark.background} colors={[semanticColors.dark.background]} />
  <ColorItem title="Surface" subtitle={semanticColors.dark.surface} colors={[semanticColors.dark.surface]} />
  <ColorItem title="Surface Raised" subtitle={semanticColors.dark.surfaceRaised} colors={[semanticColors.dark.surfaceRaised]} />
  <ColorItem title="Text" subtitle={semanticColors.dark.text} colors={[semanticColors.dark.text]} />
  <ColorItem title="Text Secondary" subtitle={semanticColors.dark.textSecondary} colors={[semanticColors.dark.textSecondary]} />
  <ColorItem title="Focus" subtitle={semanticColors.dark.focus} colors={[semanticColors.dark.focus]} />
</ColorPalette>

## Semantic Light Mode

<ColorPalette>
  <ColorItem title="Background" subtitle={semanticColors.light.background} colors={[semanticColors.light.background]} />
  <ColorItem title="Surface" subtitle={semanticColors.light.surface} colors={[semanticColors.light.surface]} />
  <ColorItem title="Text" subtitle={semanticColors.light.text} colors={[semanticColors.light.text]} />
  <ColorItem title="Text Secondary" subtitle={semanticColors.light.textSecondary} colors={[semanticColors.light.textSecondary]} />
  <ColorItem title="Focus" subtitle={semanticColors.light.focus} colors={[semanticColors.light.focus]} />
</ColorPalette>

## Compatibility Aliases

<ColorPalette>
  <ColorItem title="Legacy Primary" subtitle={colors.primary} colors={[colors.primary]} />
  <ColorItem title="Legacy Primary Dark" subtitle={colors.primaryDark} colors={[colors.primaryDark]} />
  <ColorItem title="Legacy Secondary" subtitle={colors.secondary} colors={[colors.secondary]} />
  <ColorItem title="Legacy Accent" subtitle={colors.accent} colors={[colors.accent]} />
</ColorPalette>

## Neutral

<ColorPalette>
  <ColorItem title="Background" subtitle={colors.background} colors={[colors.background]} />
  <ColorItem title="Background Alt" subtitle={colors.backgroundAlt} colors={[colors.backgroundAlt]} />
  <ColorItem title="Text" subtitle={colors.text} colors={[colors.text]} />
  <ColorItem title="Text Secondary" subtitle={colors.textSecondary} colors={[colors.textSecondary]} />
  <ColorItem title="White" subtitle={colors.white} colors={[colors.white]} />
  <ColorItem title="Black" subtitle={colors.black} colors={[colors.black]} />
</ColorPalette>

## Feedback

<ColorPalette>
  <ColorItem title="Success" subtitle={colors.success} colors={[colors.success]} />
  <ColorItem title="Warning" subtitle={colors.warning} colors={[colors.warning]} />
  <ColorItem title="Error" subtitle={colors.error} colors={[colors.error]} />
  <ColorItem title="Info" subtitle={colors.info} colors={[colors.info]} />
</ColorPalette>

## Usage Notes

- Use color tokens through components when possible.
- Do not introduce new hardcoded colors inside reusable components without documenting the reason.
- Treat runtime theme switching as future work until a ThemeProvider exists.
- Use semantic contrast text tokens for saturated action backgrounds.
